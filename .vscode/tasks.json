{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "make", // Use the appropriate command for your Makefile
            "args": ["re"], // Pass the target as an argument
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}

